"""Project package.

This repo originally kept modules at the repository root. We provide a `src`
package so imports like `from src.pdf_parsing_pipeline import ...` work
consistently for scripts/tests and for the FastAPI app.
"""

